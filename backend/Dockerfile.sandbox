FROM alpine:3.19

# Instalăm doar ce chiar există: g++ pentru compilare, python3 pentru scripturi
RUN apk add --no-cache g++ build-base python3

# Creăm un user simplu elev fără drepturi de root
RUN adduser -D -g "" elev

WORKDIR /sandbox

RUN chown -R elev:elev /sandbox

USER elev